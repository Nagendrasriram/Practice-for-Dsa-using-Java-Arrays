Blog
Discussion
Longest Subarray with given Sum K(Positives)


41

Problem Statement: Given an array nums of size n and an integer k, find the length of the longest sub-array that sums to k. If no such sub-array exists, return 0.

Examples
Example 1:
Input:
 nums = [10, 5, 2, 7, 1, 9], k = 15  
Output:
 4  
Explanation:
 The longest sub-array with a sum equal to 15 is [5, 2, 7, 1], which has a length of 4. This sub-array starts at index 1 and ends at index 4, and the sum of its elements (5 + 2 + 7 + 1) equals 15. Therefore, the length of this sub-array is 4.

Example 2:
Input:
 nums = [-3, 2, 1], k = 6  
Output:
 0  
Explanation:
 There is no sub-array in the array that sums to 6. Therefore, the output is 0.
// Online Java Compiler
// Use this editor to write, compile and run your Java code online


Done using two pointer where we try to shrink the window size using left and right where when Sum>k(expected value we try to decrase sum value from left side and always add from right side) we use while so till sum<k if sum==k we try to get the max length 
class Main {
    public static void main(String[] args) {
        int[] nums = {10, 5, 2, 7, 1, 9};
        int k = 15,sum=0,cnt=0;
        int left=0,right=0,maxLen=0;
       
        for(right=0;right<nums.length;right++)
        {
            sum+=nums[right];
            while(sum>k)
            {
                sum-=nums[left];
                left++;
            }
            if(sum==k)
            {
                maxLen=Math.max(maxLen,right-left+1);
                System.out.println(maxLen);
            }
            
        }
    }
}
